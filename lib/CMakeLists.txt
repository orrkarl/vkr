CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

ADD_LIBRARY(Nraster SHARED src/rendering/Render.cpp)

TARGET_LINK_LIBRARIES(Nraster
	PRIVATE ${OPENGL_LIBRARIES}
	PRIVATE ${GLEW_LIBRARIES})

TARGET_INCLUDE_DIRECTORIES(Nraster
	PRIVATE ${OPENGL_INCLUDE_DIRS}
	PRIVATE ${GLEW_INCLUDE_DIRS}
	PRIVATE ${PROJECT_SOURCE_DIR}/lib/inc)

SET(NRASTER_INCLUDE_DIRS ${PROJECT_SOURCE_DIR}/lib/inc/public/)

